import {Router} from 'express';import {z} from 'zod';import {prisma} from '../lib/prisma.js';import {authenticate,authorize} from '../middleware/auth.js';import {AppError} from '../lib/errors.js';const router=Router();
router.get('/',async(req,res)=>{const q=z.string().optional().parse(req.query.q);const doctors=await prisma.doctor.findMany({where:{user:{status:'ACTIVE'},OR:q?[{specialization:{contains:q}},{user:{firstName:{contains:q}}},{user:{lastName:{contains:q}}}]:undefined},include:{user:{select:{firstName:true,lastName:true,avatarUrl:true}},hospital:true,department:true},orderBy:{averageRating:'desc'}});res.json({success:true,data:doctors})});
router.get('/:id',async(req,res)=>{const doctor=await prisma.doctor.findUnique({where:{id:req.params.id},include:{user:{select:{firstName:true,lastName:true,avatarUrl:true}},hospital:true,department:true,availability:true,reviews:{take:10,orderBy:{createdAt:'desc'}}}});if(!doctor)throw new AppError(404,'Doctor not found');res.json({success:true,data:doctor})});
router.put('/me/availability',authenticate,authorize('DOCTOR'),async(req,res)=>{const slots=z.array(z.object({dayOfWeek:z.number().int().min(0).max(6),startTime:z.string().regex(/^\d{2}:\d{2}$/),endTime:z.string().regex(/^\d{2}:\d{2}$/),slotMinutes:z.number().int().min(10).max(180)})).parse(req.body);for(const s of slots)if(s.startTime>=s.endTime)throw new AppError(422,'Availability end time must be after start time');const doctor=await prisma.doctor.findUniqueOrThrow({where:{userId:req.auth!.userId}});await prisma.$transaction([prisma.doctorAvailability.deleteMany({where:{doctorId:doctor.id}}),...slots.map(s=>prisma.doctorAvailability.create({data:{doctorId:doctor.id,...s}}))]);res.json({success:true,data:slots})});export default router;
